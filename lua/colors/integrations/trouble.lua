local colors = require('colors').load()

return {
  TroubleText = { foreground = colors.light_grey },
  TroubleFoldIcon = { foreground = colors.yellow, bold = true },
  TroubleLocation = { foreground = colors.grey },
}
