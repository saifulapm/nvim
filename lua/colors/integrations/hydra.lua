local colors = require('colors').load()

return {
  HydraRed = { foreground = colors.red },
  HydraBlue = { foreground = colors.blue },
  HydraTeal = { foreground = colors.teal },
  HydraPink = { foreground = colors.violet },
}
