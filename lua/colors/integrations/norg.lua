local colors = require('colors').load()

return {
  Blue = { foreground = colors.cyan },
  Yellow = { foreground = colors.yellow },
  Red = { foreground = colors.red },
  Green = { foreground = colors.green },
  Brown = { foreground = colors.orange },
}
